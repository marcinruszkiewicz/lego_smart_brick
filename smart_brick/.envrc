# Force building rustler_btleplug from source (needed for OTP 28+ NIF compat
# and for our local write_characteristic/read_characteristic NIF patch)
export RUSTLER_BTLEPLUG_BUILD=true

# Suppress verbose Rust NIF logs (btleplug/central_manager discovery spam)
export RUST_LOG=error
